steps:
  
  - script: cat /opt/oracle/notification_end.out | mail -s "TeamConnect $ManagedServerName Restart Notice " -S from=no-reply@mitratech.com sunny.kathuria@mitratech.com
   
    displayName: 'Send Completion Email'
    env:
      ManagedServerName: ${{ parameters.client }}
